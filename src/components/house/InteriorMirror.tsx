
/**
 * Live planar FBO mirror is **disabled** (secondary gl.render blacked the
 * full canvas). Phase A/B helpers remain in `src/lib/mirrorMath.ts` and
 * `src/lib/glOffscreen.ts` for a future safe re-entry.
 *
 * Runtime vanity glass is back to MeshStandardMaterial in SenmenDisplay.
 */

export {};
