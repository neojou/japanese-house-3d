/**
 * Extra R3F clads for UB. Type-M liner is the hero GLB (`TubDisplay`);
 * `UB_BATH.clad.*` flags stay off so we do not z-fight the overlay.
 */
export function BathFinishes() {
  return <group name="bath-finishes" />;
}
