# senmen-basin (Path B)

Runtime asset: **`basin.glb`**

Regenerate (AI-unattended):

```bash
npm run bake:senmen-basin
```

Blender is optional (`tools/dcc/senmen_basin.py`). Without it the Node DCC in `src/lib/vesselBasin.ts` writes this file.

Do not replace with a box stack. No trademarks.
